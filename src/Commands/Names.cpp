#include "../includes/Command.hpp"

void Command::names(std::string leftovers)
{
	std::cout << "++++++++++++ je suis dans names voici le leftorvers : " << leftovers << " +++++++++++++" << std::endl;
	// for(std::vector<Client*>::iterator it = ; it != )

	if (!leftovers.empty())
		std::cerr << "Option is not valid" << std::endl;
	else
	{
		//lister tous les noms des utilisateurs.
		// GET CONNECTEDDDDD
	}
}
